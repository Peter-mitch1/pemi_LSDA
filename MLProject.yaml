name: Wind_Power_Prediction

conda_env: conda.yaml

entry_points:
  main:
    parameters:
      max_depth: {type: int, default: 4}
      min_samples_split: {type: int, default: 15}
    command: "python main.py --max_depth {max_depth} --min_samples_split {min_samples_split}"